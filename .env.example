# Copy to ".env" — never commit the real .env.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=books_api
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Generate with: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=30d36a3087d0f37c6b65a475f92977d260e4be5c05054914c05f15dade31ab29
JWT_TTL=3600
JWT_ISSUER=books-api

# Comma-separated origins allowed by CORS. Leave blank to fall back to '*'.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080

# Rate limiting on /auth/login: max attempts per WINDOW_SECONDS per IP.
LOGIN_RATE_LIMIT=5
LOGIN_WINDOW_SECONDS=60

APP_DEBUG=false
